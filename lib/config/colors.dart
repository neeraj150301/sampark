import 'package:flutter/material.dart';

const dBackgroundColour = Color(0xFF121212); // Darker background for depth.
const dPrimaryColour = Color(0xFFBB86FC); // Soft purple for a futuristic look.
const dContainerColour = Color(0xFF1F1B24); // Slightly lighter for card backgrounds.
const dOnBackgroundColour = Color(0xFFE0E0E0); // Light gray for text for readability.
const dOnContainerColour = Color(0xFF9E9E9E); //
